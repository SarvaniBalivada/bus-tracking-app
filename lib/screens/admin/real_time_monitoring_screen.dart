import 'package:flutter/material.dart';

class RealTimeMonitoringScreen extends StatelessWidget {
  const RealTimeMonitoringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Real-time Monitoring'),
      ),
      body: const Center(
        child: Text('Real-time Monitoring Screen - To be implemented with Google Maps'),
      ),
    );
  }
}